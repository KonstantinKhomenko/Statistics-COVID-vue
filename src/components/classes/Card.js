export default class Card {
  constructor(title, bgColor, amount, amountNew, icon) {
    this.title = title;
    this.bgColor = bgColor;
    this.amount = amount;
    this.amountNew = amountNew;
    this.icon = icon;
  }
}
